# OS
.DS_Store
Thumbs.db

# Source data (not deployed)
*.xlsx
.baza_pytan.hash
gov-data/
# leftover Mac unpack dir; new packs go to gov-data/raw/
Pytania egzaminacyjne na prawo jazdy 2025/

# Python
__pycache__/
*.pyc
venv/

# IDE
.vscode/
.idea/
.cursor/

# OneDrive/editor conflict copies
*-NODE-Oczkowscy.*

# Backblaze keys (never commit)
.b2env
scripts/.b2env

# Media (hosted on Backblaze B2, not in git)
src/media/img/
src/media/vid/

# Node / Playwright
node_modules/
test-results/
playwright-report/

# Codex local state
.codex/

# Local-only flags (never GitHub / Pages). On the live server: src/local.json
# Honored only on localhost. Example:
# { "learnQuestionJump": true, "mediaBase": "media", "offlineDownload": "packs" }
# mediaBase: "media" = src/media; "cdn" = Backblaze stream (like github.io).
# offlineDownload: "packs" = R2 zips; "files" = old per-file B2 fetch.
# packsBase: optional URL override for zip host. Omit file = github.io defaults.
src/local.json
