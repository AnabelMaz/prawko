# Windows PowerShell 5.1 parses UTF-8 scripts only when they have a BOM.
# Keep the EF BB BF prefix in the committed bytes; tests/ps1-utf8-bom.spec.js checks it.
*.ps1 text eol=crlf
# Linux/macOS installers and pipeline scripts.
*.sh text eol=lf
